# import my_hypotheses as hyp
import pandas as pd
# hypotheses = hyp.ProgressivePlayerCanBeACheater()

# df = hypotheses.get_df('blitz')

# print(df)

df = pd.DataFrame(columns=['Code'])
for i in data:
    df.merge(my_value, on='Code', how='outer') 